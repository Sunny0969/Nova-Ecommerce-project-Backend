# Web Push Notifications — Environment

# Generate keys: node scripts/generate-vapid-keys.js
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:orders@bazaar-pk.com
SERVICE_WORKER_PATH=/service-worker.js

# Must match frontend public key
# REACT_APP_VAPID_PUBLIC_KEY=<same as VAPID_PUBLIC_KEY>
